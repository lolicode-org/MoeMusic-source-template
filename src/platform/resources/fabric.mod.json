{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {},
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${fabric_entrypoint}"
    ]
  },
  "depends": {
    "fabricloader": "*",
    "minecraft": "*",
    "moemusic": ">=${moemusic_version}"
  }
}
